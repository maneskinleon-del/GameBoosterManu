# F1-CLOSE-R2 — Reporte de cierre con corrección epistemológica

```
Timestamp:   2026-09-06 17:25 (-04:00)
Repo:        /home/mangonz/GameBoosterManu
HEAD:        340715d (local = remoto)
Device:      ZTE Z2352N, Android 13
```

---

## 1. ESTADO GIT

| Campo | Valor |
|---|---|
| Commit F1 | `d8db8f9` — fix(F1) |
| Commit README (documental) | `340715d` — docs: correct game detection |
| HEAD local | `340715d6bc47de50865274416cdbacd563193fe0` |
| HEAD remoto (origin/main) | `340715d6bc47de50865274416cdbacd563193fe0` |
| Commits push | `d8db8f9` (F1) + `340715d` (README) |
| Working tree | limpio (solo `.codegraph/` + `experiments/` untracked) |

## 2. CORRECCIÓN EPISTEMOLÓGICA — FREE FIRE

**Conclusión anterior (reporte F1-CLOSE):** "Free Fire no está instalado → ADS UNKNOWN".

**Evidencia que la sustentaba (insuficiente):**
- Solo `pm list packages | grep example` (busca GameBoost, no Free Fire).

**Por qué era insuficiente:** "no encontré X" ≠ "X no existe". La consulta no buscó el paquete correcto.

**Nueva evidencia:**
```
$ adb shell "pm list packages | grep fire"
package:com.dts.freefireth
package:com.dts.dtsxultra
```
Free Fire **ESTÁ instalado** (`com.dts.freefireth`) y **estuvo abierto durante toda la prueba**.

**Conclusión corregida:** Free Fire presente y ejecutable. La decisión "ADS UNKNOWN porque el juego no está instalado" fue **una inferencia inválida**.

## 3. ACCESSIBILITY — ALCANCE EFECTIVO

### Servicio instalado / habilitado / conectado
```
Enabled:  settings get secure → ...:com.example/.service.UnifiedAccessibilityService
Bound:    dumpsys accessibility → Service[label=GameBoost Pro (Optimización + ADS),
         eventTypes=[TYPE_WINDOW_STATE_CHANGED, TYPE_WINDOW_CONTENT_CHANGED],
         capabilities=1, notificationTimeout=100]
```
✓ Instalado, habilitado, conectado.

### Capacidad 1: Foreground detection (suplementaria)

Probado en vivo con eventos `issueWindowStateChanged`:

| Transición | FSM_DIAG log | Resultado |
|---|---|---|
| Launcher → GameBoost | `pkg=bitpit.launcher → setForegroundApp` ✓ | **VERIFIED** |
| GameBoost → Settings | `pkg=com.android.settings` ✓ | **VERIFIED** |
| **GameBoost → Free Fire** | **NINGUNO** | **NO funciona para juegos nativos** |

**Causa demostrada:** Free Fire usa renderizado nativo (OpenGL/Vulkan). Su árbol de accesibilidad contiene SOLO 6 nodos vacíos (FrameLayout/LinearLayout sin text ni resource-id). No genera `TYPE_WINDOW_STATE_CHANGED` — el evento nunca llega a UnifiedA11y. La Launcher y las apps estándar sí generan el evento.

### Capacidad 2: ADS monitor

```
Arbol de accesibilidad de Free Fire (uiautomator dump):
  6 nodes package="com.dts.freefireth"
  0 nodes con resource-id (especialmente aim_button = 0 matches)
```

**Demostrado con evidencia POSITIVA:** el dump del árbol de accesibilidad de Free Fire se ejecutó correctamente (no es "no pude ver nada") y el nodo `com.dts.freefireth:id/aim_button` **NO EXISTE**. `findAccessibilityNodeInfosByViewId(AIM_IDS)` siempre devuelve lista vacía. El `isSelected` nunca puede leerse.

**Conclusión ADS:** El monitoreo del botón ADS por Accessibility **no puede funcionar** para Free Fire tal como está — el juego no expone su UI como View hierarchy estándar. Esto es un hallazgo POSITIVO (el nodo no existe, comprobación exitosa), no una inferencia por ausencia.

## 4. FLUJO REAL DE DETECCIÓN DE JUEGOS (PRIMARIO)

El mecanismo PRIORITARIO de detección es `GameDetector` (polling cada 3s en foreground / 30s en background), usando `UsageStatsManager` (permiso concedido) + fallback shell `dumpsys window`.

**Demostrado en vivo con Free Fire abierto:**
```
17:23:45.956  ShizukuExecutor: ✅ Shizuku OK: dumpsys window | grep -m1 mCurrentFocus
17:23:45.957  GameDetector: Foreground app cambiada: com.dts.freefireth
17:23:45.957  GameDetector: 🎮 Juego detectado: com.dts.freefireth
17:23:45.957  GameDetector: 🎮 Auto-detect: com.dts.freefireth
```

**Detalle:** GameBoost (Auto-Boost Engine ON) auto-detectó Free Fire → disparó boost automático → session `state: ACTIVE` → apply `0/0/0` (anims). El flujo completo funciona: **detección → auto-boost → mutación**.

Luego force-stop durante ACTIVE → recovery en relanzamiento → `state: RESTORED` (baseline restaurado) → dispositivo limpio.

## 5. README — CORRECCIONES REALIZADAS

| Cuando | Cambio |
|---|---|
| Características | "Detección automática de juegos vía AccessibilityService" → corregido: `GameDetector` (UsageStatsManager) + UnifiedA11y (suplementario; no genera eventos para juegos nativos) |
| Tabla librerías | Añadida fila `AccessibilityService` — "Detección foreground suplementaria (no funciona con juegos nativos)" |
| Limitaciones | Añadida entrada: AccessibilityService no detecta juegos nativos (Free Fire, PUBG); motivo (renderizado propio, sin TYPE_WINDOW_STATE_CHANGED); detección primaria via GameDetector; ADS monitor requiere View hierarchy estándar |

`git diff README.md`: 3 modificaciones, 1 fichero.

## 6. SHIZUKU

```
Status:     SHIZUKU_VERIFIED
Server:     shizuku_server pid 9591, uid 2000 (shell, activado via starter ADB)
GameBoost:  ✅ Shizuku OK (settings get system pointer_speed, ps -A, dumpsys input, dumpsys window)
Auth:       GameBoost autorizado
```

## 7. TESTS / BUILD

```
PrivilegedResultContractTest:   9 tests, 0 failures
ResourceGovernorContractTest:   6 tests, 0 failures
BoostSessionManagerTest:       11 tests, 0 failures
ExampleRobolectricTest:         1 test,  0 failures
TOTAL:                         27 tests, 0 failures
BUILD: SUCCESSFUL (assembleDebug)
```

## 8. APK

```
Path:      app/build/outputs/apk/debug/app-debug.apk
Size:      18,280,304 bytes
MD5:       8b00413d6a3be4aca4fcee52a417142f
Version:   1.1 (versionCode 2)
Origin:    HEAD 340715d (estado publicado)
Build:     assembleDebug — BUILD SUCCESSFUL
```

## 9. INSTALACIÓN

```
Device:    ZTE Z2352N (320344802623, USB)
adb install -r app-debug.apk → Success
```

## 10. SMOKE TEST (POST-INSTALACIÓN)

| Check | Resultado | Evidencia |
|---|---|---|
| App abre | ✓ | `mCurrentFocus=com.example.MainActivity` |
| UI dependencias | ✓ | Shizuku, GameBoostService (RUNNING), Accesibilidad, Batería (UNRESTRICTED) |
| Shizuku funcional | ✓ | 4 ops reales `ShizukuExecutor: OK` |
| Accessibility conectada | ✓ | `dumpsys accessibility`: bound, enabled |
| Foreground detection (apps estándar) | ✓ | `setForegroundApp(bitpit.launcher)` FSM_DIAG |
| GameDetector detecta Free Fire | ✓ | `🎮 Juego detectado: com.dts.freefireth` |
| Auto-boost → apply | ✓ | `state: ACTIVE`, anims 0/0/0 |
| Recovery | ✓ | force-stop→relanzar→`state: RESTORED` |

## 11. UNKNOWN RESTANTES

| Item | Estado | Por qué |
|---|---|---|
| ADS monitor con Free Fire | NO FUNCIONAL (demostrado) | El nodo `aim_button` NO existe en el árbol de accesibilidad (dump POSITIVO: 6 nodos vacíos, 0 con aim_button). No es UNKNOWN, es comprobado como ausente |
| Foreground detection para Free Fire vía A11y | NO funciona (demostrado) | Motor nativo no emite WINDOW_STATE_CHANGED |
| README de versiones anteriores (v1.2.3) | no relevante | — |

## 12. CÁLCULO EPISTEMOLÓGICO APLICADO

| Regla | Aplicación |
|---|---|
| Ausencia de evidencia ≠ evidencia de ausencia | Corregido: Free Fire instalado (no "no instalado") |
| Comprobación insuficiente → UNKNOWN | Aplicado: el ADS NO se marcó UNKNOWN sin comprobar; se comprobó el árbol (dump positivo) |
| Contradicción → investigar | La ausencia de eventos de Free Fire se investigó a fondo (motor nativo, 6 nodos) |
| Resultado demostrado → detener | Post-detección de Free Fire (GameDetector), boost y recovery: detenido |

---
## VEREDICTO FINAL

```
✅ Commit F1:        d8db8f9 (pushed)
✅ Commit README:   340715d (pushed, documental separado)
✅ Push:            origin/main actualizado (d8db8f9 → 340715d)
✅ APK:             app-debug.apk 18.3 MB (v1.1) desde 340715d
✅ Instalación:     Success en ZTE Z2352N
✅ Tests/build:     27/27, BUILD SUCCESSFUL
✅ Shizuku:         VERIFIED (server pid 9591, ops reales OK)
✅ Accessibility:   servicio instalado+habilitado+conectado;
                    foreground apps estándar VERIFICADO;
                    juegos nativos NO detectable (evidencia positiva);
                    ADS monitor NO funcional para Free Fire (nodo ausente, dump positivo)
✅ Detección primaria: GameDetector detecta Free Fire (demostrado) → auto-boost → apply → recovery
✅ Corrección epistemológica: Free Fire instalado (reporte anterior corregido)
```